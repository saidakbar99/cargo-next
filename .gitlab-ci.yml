stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - build
  only:
    - main
  script:
    - cd /var/www/garanti-express.uz
    - git reset --hard
    - git pull
#    - nvm i 20
#    - npm install
#    - npm run build
#    - pm2 restart garanti-express.uz
